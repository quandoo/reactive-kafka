kafka:
  bootstrap-servers: localhost:9092
  consumer:
    max-pool-interval-millis: 2000
    parallelism: 1
    batch-size: 10
    auto-offset-reset: earliest
    group-id: test
  producer:
    max-in-flight: 10